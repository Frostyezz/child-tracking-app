import { createStyles } from "@mantine/core";

export const useSimpleBackIconButtonStyles = createStyles((theme) => ({
  backButton: {
    position: "absolute",
    top: 15,
    left: 15,
    zIndex: 1,
  },
}));
